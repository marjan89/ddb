# Epic M / M-4 — XML demo variant of the Compose TC (mirror of ../tests/t31.yaml).
# Element IDs follow the M-1 scaffold convention: T<N>_<element-name> as android resource id.
# Runner: install ddb/e2e/demo-app-xml/ and target package io.substrate.regdemo.xml.
app: io.substrate.regdemo.xml
id: TC-T31-assert-hint
name: "T31: assert hint key (StepRaw.hint → AssertStep) — parser flows hint metadata"
steps:
  - action: wait_idle
    seconds: 2
  - assert: element_exists
    target: {content_fuzzy: "T1 Launch"}
    hint: "HomeScreen first NavigationLink — sanity baseline"
  - action: capture
    output: "/Users/Shared/projects/substrate-distro/ddb/e2e/captures/t31-semantic.yaml"
